import{c as t,o as e,g as n}from"./vendor.9944df43.js";const a={class:"markdown-body"},s=n('<h1>Key spec</h1><h2>Introduction</h2><p>A Univrse Key is a CBOR data structure that represents a cryptographic key. Univrse Keys closely mirror JSON Web Keys, and it should prove simple to convert keys between the two specifications.</p><p>Keys are used in the <a href="/docs/signature">Univrse Signature</a> and <a href="/docs/recipient">Univrse Recipient</a> specifications.</p><h2>Structure</h2><p>A Univrse Key described using Concise Data Definition Language:</p><pre><code class="language-elixir">Key <span class="token operator">=</span> <span class="token punctuation">{</span>\n  <span class="token attr-name">kty:</span> text<span class="token punctuation">,</span>\n  ? <span class="token attr-name">crv:</span> text<span class="token punctuation">,</span>\n  ? <span class="token attr-name">x:</span> bytes<span class="token punctuation">,</span>\n  ? <span class="token attr-name">y:</span> bytes<span class="token punctuation">,</span>\n  ? <span class="token attr-name">k:</span> bytes<span class="token punctuation">,</span>\n  ? <span class="token attr-name">d:</span> bytes\n<span class="token punctuation">}</span>\n</code></pre><h2>Key types</h2><p>The following values are valid key types (<code>kty</code>):</p><ul><li><code>EC</code> - Elliptic curve key</li><li><code>oct</code> - Octet sequence</li></ul><h3><code>EC</code> - Elliptic curve key</h3><p>Elliptic curve keys can be used for signature algorithms and asynchronous encryption algorithms. The key parameters are:</p><table><thead><tr><th>Param</th><th>Description</th><th>Required</th></tr></thead><tbody><tr><td><code>kty</code></td><td>Key type</td><td><code>EC</code></td></tr><tr><td><code>crv</code></td><td>Elliptic curve</td><td>✅</td></tr><tr><td><code>x</code></td><td>X coordinate</td><td>✅</td></tr><tr><td><code>y</code></td><td>Y coordinate</td><td>✅</td></tr><tr><td><code>d</code></td><td>Private key</td><td></td></tr></tbody></table><h3><code>oct</code> - Octet sequence</h3><p>Octet sequence keys can be used for HMAC algorithms and synchronous encryption algorithms. The key parameters are:</p><table><thead><tr><th>Param</th><th>Description</th><th>Required</th></tr></thead><tbody><tr><td><code>kty</code></td><td>Key type</td><td><code>oct</code></td></tr><tr><td><code>k</code></td><td>Private key</td><td>✅</td></tr></tbody></table><h2>Serialisation</h2><p>A Univrse Key can be serialised into a single CBOR-encoded binary. When encrypting Envelopes for two or more recipients, the content encryption key is serialised, and then encrypted for each recipient.</p>',18),o={expose:[],setup:n=>(n,o)=>(e(),t("div",a,[s]))};export default o;
